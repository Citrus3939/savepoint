# AGENTS.md

## Cursor Cloud specific instructions

This is a nascent repository ("savepoint — a manuscript of life respawn") with no application code, services, or dependencies yet. The repository contains only a `README.md`.

- **No build/lint/test commands exist** — there are no `package.json`, `Makefile`, `requirements.txt`, or other dependency manifests.
- **No services to start** — there is no backend, frontend, or database to run.
- As application code is added, update this file with relevant build, test, lint, and run instructions.
